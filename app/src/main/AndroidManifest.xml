<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
  package="tech.devrocks.oink">

  <application
    android:allowBackup="true"
    android:icon="@drawable/ic_oink_lineal_color"
    android:label="@string/app_name"
    android:roundIcon="@drawable/ic_oink_lineal_color"
    android:supportsRtl="true"
    android:theme="@style/AppTheme">
    <activity android:name=".ui.CategoriaActivity">

    </activity>
    <activity android:name=".ui.ContasBancariasActivity"></activity>
    <activity android:name=".ui.PoteGastoActivity"></activity>
    <activity android:name=".ui.FormasPagamentoActivity" />
    <activity android:name=".ui.RegistroFinanceiroActivity">

    </activity>

    <activity android:name=".ui.MainActivity">
      <intent-filter>
        <action android:name="android.intent.action.MAIN" />
        <category android:name="android.intent.category.LAUNCHER" />
      </intent-filter>
    </activity>
  </application>

</manifest>